language: cpp

install:
  - pip install -U cpplint
  - sudo apt-get install libboost-all-dev

compiler:
  - gcc
  - clang

before_script:
  - mkdir build
  - cd build
  - cmake ..

script: make && make test
